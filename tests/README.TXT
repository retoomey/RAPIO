//Separate build folder for Unit tests for RAPIO using BOOST
//These are tests can use to test the core after changes

Build with:
make check

//To run all tests:
./testrapio

// Verbose:
./testrapio --log_level=test_suite

